/**
 * dsh-plugin-prompt-templates — node half.
 * Pure client plugin: 空 apply 让插件出现在 Loader /「设置 → 插件」清单；
 * 浏览器半边经 package.json 的 dsh.client + exports["./client"] 自动装载。
 */
function apply() {}
export { apply };
